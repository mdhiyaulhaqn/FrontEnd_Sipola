export default class User {
    constructor(username, email, password, role, noHP, alamat, name) {
      this.username = username;
      this.email = email;
      this.password = password;
      this.role = role;
      this.noHP = noHP;
      this.alamat = alamat;
      this.name = name;
    }
  }